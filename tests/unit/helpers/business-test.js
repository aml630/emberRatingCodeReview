import { business } from 'ember-rating-code-review/helpers/business';
import { module, test } from 'qunit';

module('Unit | Helper | business');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = business([42]);
  assert.ok(result);
});
