import Ember from 'ember';
// import moment from 'moment';


// export function formatDate(params/*, hash*/) {
//   console.log(params);
//   var newDate = moment().format(params)
//   return newDate;
// }

export default Ember.Helper.helper(formatDate);
